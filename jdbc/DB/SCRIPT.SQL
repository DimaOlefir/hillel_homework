CREATE TABLE CITY(
  ID_CITY INT(5) AUTO_INCREMENT PRIMARY KEY,
  NAME VARCHAR(10) NOT NULL
);

CREATE TABLE STUDENT(
  ID_STUDENT INT(5) AUTO_INCREMENT PRIMARY KEY,
  FIRSTNAME VARCHAR(10) NOT NULL ,
  LASTNAME VARCHAR(10) NOT NULL,
  CITY_ID INT(5) NOT NULL,
  FOREIGN KEY (CITY_ID) REFERENCES CITY (ID_CITY)
);

INSERT INTO CITY (NAME) VALUES
  ('Odessa'),
  ('Kiev');

INSERT INTO STUDENT (FIRSTNAME, LASTNAME, CITY_ID) VALUES
  ('Petya', 'Pyatochkin', 1),
  ('Vasiliy', 'Pupkin', 1);